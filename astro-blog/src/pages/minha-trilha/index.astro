---
import { getCollection } from "astro:content";
import getSortedPrivatePosts from "@utils/getSortedPrivatePosts";
import getPagination from "@utils/getPagination";
import LearningPath from "@layouts/LearningPath.astro";

const posts = await getCollection("blog");

const sortedPosts = getSortedPrivatePosts(posts);

const pagination = getPagination({
  posts: sortedPosts,
  page: 1,
  isIndex: true
});
---

<LearningPath {...pagination} />

<script>
  console.log('HEELLOOOWW');
</script>